#include <stdio.h>
#include "main.h"

/**
 * _abs -verfi si int vas me retourner une valeur absolu
 * @num : variable entier
 * Return: 0
 */

int _abs(int num)
{
	if (num >= 0)
	return (0);

	else
		return (-0);
}
